package Section09.AbstractClasses.Section01.AbstractClasses;

/**
 * @author Raffi
 */

/*Since IntelliJ clearly marks interfaces with an I in the Project folder, it is unnecessary to
* name it starting with an I. Furthermore, it is smarter to create this interface rather than have fly() as an
* abstract class because birds aren't the only animals that can fly.*/

public interface CanFly {
    void fly();

}
